export interface DokobitDocumentMetadata {
  createdBy: string;
  createdOn: string;
  deadline: string;
}

export interface DokobitDocumentComment {
  id: string;
  addedBy: string;
  addedOn: string;
  text: string;
}
